import { Text } from "./doc"

export type CostFactory = null

export type Cost = null

export const addCosts = (a: Cost, b: Cost): Cost => null

export const costText = (doc: Text, cf: CostFactory): Cost => {
  throw "todo"
}
