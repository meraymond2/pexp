import { CostFactory } from "./cost";
import { Document } from "./doc";
import { MeasureSet } from "./measure";

export const resolve = (
  document: Document,
  costFactory: CostFactory,
  computationWidthLimit: number
): MeasureSet => null
